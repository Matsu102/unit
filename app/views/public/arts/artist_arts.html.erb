<h2>Artist's Arts <%= link_to @user.handle_name, artist_path(@user.id) %>の作品一覧</h2>

<% @arts.each do |art| %>
  <%= link_to art_path(art.id) do %>
    <%= image_tag art.image %>
  <% end %>
<% end %>